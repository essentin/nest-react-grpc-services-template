query getAllInvites($currentPage: Int, $searchList: String){
    getAllInvites(currentPage: $currentPage, searchList: $searchList)  {
      results{
        id
        userId
        email
        firstName
        inviteStatus
      }
      count
      status
      errorMessage
      
    }
  }